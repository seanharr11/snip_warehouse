from setuptools import setup, find_packages

setup(name="snip_warehouse",
      version="0.1",
      description="Warehouse DbSNP Data and direct2consumer genomics tests",
      packages=find_packages()
      )
